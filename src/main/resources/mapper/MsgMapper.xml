<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lanfangyi.nettyim.mapper.MsgMapper">
    <sql id="columns">
      id,userid,usertype,frienduserid,friendusertype,type,data,status,ctime,utime
    </sql>

    <select id="findById" resultType="com.lanfangyi.nettyim.bean.Msg">
        select id,<include refid="columns"/>
        from msgs
        where id=#{id}
    </select>

    <insert id="insert" parameterType="com.lanfangyi.nettyim.bean.Msg">
        insert into msgs(<include refid="columns"/>)
        values(#{id}, #{userid}, #{usertype}, #{usertype},#{frienduserid},#{friendusertype},
        #{type},#{data},#{status},#{cTime},#{uTime})
    </insert>

    <delete id="deleteById">
    	delete from msgs
    	where id in
    	<foreach collection="array" item="id" index="index" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

</mapper>